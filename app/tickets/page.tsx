import TicketsList from "./TicketsList"

const Tickets = () => {
  return (
    <main>
      <nav>
        <h2>tickets</h2>
        <p><small>Currently open tickets</small></p>
      </nav>
        <TicketsList />
    </main>
  )
}

export default Tickets
